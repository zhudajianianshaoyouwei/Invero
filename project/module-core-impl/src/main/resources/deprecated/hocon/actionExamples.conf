{
  if: has money 500
  then:
    inline-command "eco take {{context &price}}"
  else: {
    when: luckperms groupName
    default = tell default group
    svip = tell svip group
    else = fuck off
  }
}